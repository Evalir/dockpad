<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Dockpad.Views.EditActivityPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10">
                <Label Text="Name" HorizontalOptions="StartAndExpand" FontSize="16"></Label>
                <Entry Text="{Binding Form.Name, Mode=TwoWay}"></Entry>

                <Label Text="Description" HorizontalOptions="StartAndExpand" FontSize="16"></Label>
                <Entry Text="{Binding Form.Description, Mode=TwoWay}"></Entry>

                <Label Text="Is Active?" HorizontalOptions="StartAndExpand" FontSize="16"></Label>
                <Entry Text="{Binding Form.IsActive, Mode=TwoWay}"/>

                <Label Text="Last time" HorizontalOptions="StartAndExpand" FontSize="16"></Label>
                <Entry Text="{Binding Form.LastTime, Mode=TwoWay}" Keyboard="Numeric"></Entry>

                <Button Text="Save" Command="{Binding SaveCommand}" Style="{StaticResource dpNormalButton}" HorizontalOptions="Center"></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>